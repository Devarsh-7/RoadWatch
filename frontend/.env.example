# ==========================================
# RoadWatch Frontend Environment Variables
# ==========================================

# Backend API base URL
# Leave empty in local development so Vite's proxy automatically routes `/api` to http://127.0.0.1:8000
# For production or separate backend hosting, set to: https://your-backend-domain.com
VITE_API_URL=
